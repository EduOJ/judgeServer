on: [push]
name: Test
jobs:
  test:
    strategy:
      matrix:
        go-version: [1.14.x]
        platform: [ubuntu-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Install Go
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go-version }}
      - name: Add User
        run: |
          sudo useradd script_user
          sudo useradd compile_user
          sudo useradd run_user
          sudo useradd compare_user
      - name: Echo User
        run: sudo whoami
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.push.head.sha }}
      - name: Go Test
        run: sudo go test ./...
  test-race:
    strategy:
      matrix:
        go-version: [1.14.x]
        platform: [ubuntu-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Install Go
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go-version }}
      - name: Add User
        run: |
          sudo useradd script_user
          sudo useradd compile_user
          sudo useradd run_user
          sudo useradd compare_user
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.push.head.sha }}
      - name: Go Test Race
        run: go test ./... -race
